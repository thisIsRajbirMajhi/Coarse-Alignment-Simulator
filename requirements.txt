numpy==2.5.2
opencv-python==5.0.0.93
PyQt5==5.15.11
# Optional: for packaging and testing
# pytest>=8.0
# pyinstaller>=6.0
# pyqtgraph==0.14.0  # unused — graph removed, metrics only (was dead dep, now optional)
