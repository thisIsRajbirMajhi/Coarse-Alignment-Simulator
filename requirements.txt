# Core simulator (GUI + synthesis) — pins verified against test env (Python 3.14, 97 passed)
numpy>=1.26,<2.6
opencv-python>=4.8,<5.1
PyQt5==5.15.11

# Dataset inspection / training (scripts/train.py, scripts/visualize_labels.py)
pyyaml>=6.0
ultralytics>=8.3.0
torch>=2.2
onnx>=1.15
onnxruntime>=1.17
